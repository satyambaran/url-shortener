{
  "dashboard": {
    "id": null,
    "title": "URL Shortener - Basic",
    "timezone": "browser",
    "panels": [
      {
        "type": "graph",
        "title": "Request Rate (r/s)",
        "targets": [{"expr":"sum(rate(http_server_requests_seconds_count{job=\"url-shortener\"}[1m])) by (method)", "refId":"A"}]
      },
      {
        "type": "graph",
        "title": "Error Rate",
        "targets": [{"expr":"sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m]))","refId":"B"}]
      }
    ]
  }
}
